spring:
  application:
    name: good-service
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml
        group: DEFAULT_GROUP
        prefix: good-service-dev   # 与 Nacos 中的 Data ID 对应